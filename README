step 1. go into VS Code
step 2. select source control sidebar button
step 3. click repositories
step 4. click main branch name
step 5. create new branch if not already there
step 6. type <your-initials/name-here>--test-branch into the new branch name
step 7. press enter/return, and then commit changes.

NOTE: make sure not to commit to main branch. to do so follow these steps
a. click source control in VS code
b. then click repositories
c. then click branch name
d. then click the test branch with YOUR NAME/initials